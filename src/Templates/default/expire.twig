
    gzip on;

    location ~* \.(?:jpg|jpeg|gif|png|ico|cur|gz|svg|svgz|mp4|ogg|ogv|webm|htc)$ {
        expires 1y;
        add_header Cache-Control "public";
        access_log off;
    }

    location ~* \.(?:css|js)$ {
        expires 1y;
        access_log off;
    }
