server {
    listen          80;
    listen          [::]:80;

    server_name     {{ server_name }};

    {%- include 'default/expire.twig' %}

    {% block location %}{% endblock %}

    {%- include 'default/logs.twig' with {'access_log': access_log, 'error_log': error_log} only %}
}
